# 프로젝트 요약 — 이차전지 전극 공정 영상 자동 분석 시스템

자소서 작성용 요약. 세부 근거는 `PLAN.md`, `experiments/leak_diagnosis.md`, `experiments/relabel_progress.md` 참조.

작성일 2026-08-26

---

## 1. 한 줄 요약

**이차전지 소재 연구원 경력을 활용해, 배터리 전극 공정 영상을 자동으로 구간화·설명하는 CV+VLM 파이프라인을 설계·구현 중.** 1차 시도 실패를 수치 진단으로 원인 특정한 뒤, 아키텍처·데이터·라벨 기준을 전면 재설계했다.

---

## 2. 배경과 동기

### 나의 위치
- 이차전지 소재 전공 + 연구원 경력
- 소재 연구자 → AI 엔지니어 전환을 위한 포트폴리오 프로젝트
- 타깃: LG에너지솔루션·삼성SDI·SK온 스마트팩토리/AI 조직, 이차전지 장비사(피엔티·씨아이에스·한화모멘텀)

### 왜 배터리 공정 CV 인가
- **차별화** — YOLO + 공개 데이터셋 포트폴리오는 흔하다. 배터리 공정 CV를 소재 연구 경력자가 만든 사례는 거의 없다
- **경력 연결성** — 도메인을 바꾸면 이전 경력이 무관해진다. 배터리를 유지해야 "소재 → AI" 서사가 성립
- **평가 정당성** — VLM 출력의 사실성을 도메인 전문가로서 직접 채점할 수 있다. 이것이 다른 지원자와 갈리는 지점

---

## 3. 1차 시도 — 무엇을 했고 왜 실패했나

### 시도한 것
- 36분짜리 배터리 제조 공정 영상 1개로 YOLO 학습
- 9클래스 (coating electrode, separator, press roller, slitting blade, …)
- 136장 실사 프레임 + Gemini 생성 합성 이미지
- Roboflow 라벨링, Colab/RunPod 학습, 19회 실험

### 지표 vs 실체
- **mAP50 0.995 기록.** 그러나 미학습 영상에 적용 시 성능 무너짐
- 처음 세운 가설(train/val 누수)은 실제 검사 결과 **누수 0건**으로 오답 확인

### 진단으로 특정한 진짜 원인 (우선순위 순)

| 순위 | 원인 | 근거 |
|---|---|---|
| 1 | **라벨 기준 자체가 잘못됨** | 박스의 30%가 검은 여백을 20%+ 포함. 설비 윤곽이 아니라 "설비가 있는 영역"을 뭉뚱그림. 그 결과 모델이 "장면 분류" 수준의 큰 박스만 예측 |
| 2 | **실사 프레임이 단일 영상 출처** | 어떻게 분할해도 카메라·조명·설비 동일 → "미학습 영상 성능"을 측정할 방법이 원리적으로 없었음 |
| 3 | **val 세트가 원본 9~16장** | 클래스당 3~5장. 이미지 1장 차이로 mAP가 0.99↔0.90 뒤집힘 → 지표 신뢰성 상실 |
| 4 | **학습 데이터 36~72%가 Gemini 합성 이미지** | 실사보다 조명·구도가 균일해 "더 쉬운 문제"를 푼 결과가 mAP 0.995 |

> **"mAP 0.995는 거짓말이 아니라, 더 쉬운 문제를 푼 정답이었다."**

### 클리핑 실험으로 판정 정정
경계 이탈 박스 35% 를 처음엔 좌표 버그로 의심했으나, 클리핑 후에도 박스 면적이 원래의 84% 로 거의 변하지 않음 → **좌표 문제가 아니라 라벨 기준의 문제**로 확정. `experiments/viz/gt_real_clipped.jpg` 로 시각 검증.

---

## 4. 재설계 — 무엇을, 왜 바꿨나

### 아키텍처 재설계
**문제:** 원래 구상은 "탐지 → VLM 설명". 그러나 VLM은 프레임만 봐도 코팅 라인을 알아보고, 설명은 교과서 지식이라 프레임이 바뀌어도 출력이 동일 → **탐지기와 VLM이 서로를 필요로 하지 않음.**

**해결:** 36분 전 공정 영상이라는 자산을 강점으로 전환.

```
36분 영상
   ├─[1] 탐지기: 프레임별 설비 인식              ← VLM이 못 함
   ├─[2] 시계열 집계 → 공정 구간 분류
   ├─[3] VLM: 구간별 근거 기반 리포트            ← 탐지기가 못 함
   │       Claude(교사) → 소형 VLM(온프레미스 증류)
   └─[4] 출력: 타임라인 + 리포트 + 검색 인덱스
```

**역할 분담이 명확:** 36분 영상은 통째로 VLM에 못 넣으므로 탐지기 필수, 구조화된 탐지 결과를 사람이 읽는 서술로 바꾸려면 VLM 필수.

### 데이터 재설계

| 항목 | 1차 시도 | 재설계 |
|---|---|---|
| 데이터량 | 136장 / 영상 1개 | 600~800장 / 영상 3~5개 |
| 클래스 | 9개 (박스 부적합·혼동 포함) | **4개** (`coating_die`, `roll_press`, `slitting_knife`, `winding_core`) |
| 라벨 기준 | 설비가 있는 영역 | **설비 자체의 윤곽에만 밀착** |
| 분할 | 영상 내 시간 분리 | **영상 단위 분리** (미학습 영상으로 test) |
| 합성 이미지 | 학습에 포함 | **전면 배제** |
| 라벨링 | 전수 수작업 | 의사라벨 + 수정 반복 (self-training) |

### 클래스 축소 원칙 (3조건 모두 충족)
1. 바운딩박스로 표현 가능한가 (면·필름 제외)
2. 형태만으로 다른 클래스와 구분되나
3. **공정 구간을 특정하는 데 기여하나** ← 아키텍처 상 핵심 기준

### 도메인 지식이 알고리즘에 들어가는 지점
- **공정 순서 제약**: 전극 공정은 코팅 → 캘린더링 → 슬리팅 → 와인딩 순으로 고정 → 순서에 어긋나는 전환을 후처리로 교정
- **혼동 클래스 대응**: `roll_press` 와 `slitting_knife` 는 둘 다 금속 원통이라 사람도 헷갈릴 수 있음 → 필요 시 `roller` 로 병합하고 시간 문맥으로 구분
- **온프레미스 증류의 필요성**: 배터리 제조사는 라인 구성이 영업비밀 → 공정 영상을 외부 API로 전송 불가 → Claude 로 학습 데이터 생성 후 소형 VLM 증류가 실제 도입 요건

---

## 5. 현재까지 완료한 작업

### Phase 0 — 진단 (완료, 2026-08-15)
- `experiments/leak_diagnosis.md` 작성 — 근거 1~6을 수치로 특정
- 진단 스크립트 7개 작성: `analyze_dataset.py`, `check_boxes.py`, `check_letterbox.py`, `clip_boxes.py`, `visualize_labels.py`, `check_leakage.py`, `collect_runs.py`
- 정답 박스 시각화(`gt_real.jpg`) + 모델 예측 시각화(`pred_unlabeled.jpg`) 로 "장면 분류가 되어버렸다" 를 시각 근거로 확보

### 프레임 선별 파이프라인 (완료, 2026-08-16)
- v1_source 영상에서 4클래스 후보 345장 선별
- pHash 기반 중복 제거 + stride cap 으로 시각 유사 프레임 배제
- manifest.csv 로 프레임 출처 추적 가능하게 구조화

### 다중 영상 라벨링 계획 확정 (완료, 2026-08-19)
- `experiments/labeling_plan.md` — 영상별 라벨링 구간, 4클래스 시각적 판별 기준, train/val/test 영상 분리 원칙, test 영상 지정 근거
- 신규 영상 후보 11개 프로브 → 롱폼 4개 선정
- **분할 확정:** train = v1_source + hmhH + UHZg + shorts / val = j1jWp9WxGLM (Zeekr) / test = zbBxJLGaoys (Processlytic 4K)

### 다중 영상 확보 (부분 완료, 2026-08-19~21)
- Shorts 5개 다운로드 성공
- 롱폼 4개 + Shorts 2개는 **YouTube signature/PoT 인증 벽에 막혀 중단** — cobalt.tools 또는 Windows+Deno 우회 필요

---

## 6. 앞으로 남은 작업 (`PLAN.md` 기준)

- **Phase 2 (진행 중)** — 다운로드 재개 → 프레임 추출 → Roboflow 재라벨링 → 영상 단위 분할
- **Phase 3** — RunPod 에서 YOLOv11-s 학습, SAHI 타일링·Soft-NMS 튜닝
- **Phase 4** — 프레임 단위 공정 분류 + 시간축 평활화 + 도메인 제약 후처리
- **Phase 5-A** — Claude API 로 구간별 리포트 생성 (환각 억제 프롬프트 + JSON 스키마 고정)
- **Phase 5-B** — Qwen2.5-VL-7B LoRA 증류 (Unsloth, RunPod A100)
- **Phase 6** — 도메인 전문가 루브릭으로 3자 비교 (베이스 / 증류 / Claude), Streamlit 데모

---

## 7. 자소서에 쓸 만한 서사 포인트

### (a) 실패를 "수치로 진단"할 수 있는 사람
- mAP 0.995 라는 유리한 지표에도 "왜 실사에서 안 되는가"를 파고들어 원인 4개를 데이터로 특정
- 처음 세운 가설(누수)이 틀렸음을 스스로 검사로 반증하고 진짜 원인으로 재정의
- 클리핑 실험으로 "좌표 버그" 가설도 반증 → 라벨 기준 문제로 확정

### (b) 도메인 지식을 알고리즘 설계에 투입
- 소재 연구자로서만 판단 가능한 클래스 취사선택 (필름·면 형태 배제, 공정 구간 특정 기여도 판별)
- 공정 순서 제약을 후처리 규칙으로 코드에 반영
- VLM 출력의 사실 정확성을 직접 채점하는 루브릭 설계 — **재현 불가능한 자산**

### (c) 아키텍처의 정당성을 스스로 검증
- 처음 "탐지 → VLM 설명" 구조의 구조적 결함(둘이 서로를 필요로 하지 않음)을 자각하고 재설계
- 재설계 아키텍처가 "탐지기·VLM 각자 상대가 못 하는 일을 담당" 하는 형태로 정당화됨
- 대조 실험(프레임만 vs 프레임+탐지결과)을 설계에 포함 — 아키텍처가 실제로 기여하는지 검증

### (d) 실무 도입 조건까지 고려한 기술 선택
- 온프레미스 증류를 "기술 과시"가 아니라 **"영업비밀인 라인 영상을 외부 API로 못 보내는 실제 도입 요건"** 으로 근거화
- Claude → Qwen2.5-VL-7B 증류 경로가 왜 이 도메인의 실제 경로인지 명시

### (e) 정직한 지표 서사
- "영상 단위 분할의 mAP는 낮게 나오지만 **그것이 진짜 숫자**" 를 README 서술 포인트로 삼는 태도
- 1차 실패를 숨기지 않고 진단 → 재설계 → 결과 흐름을 그대로 문서화

---

## 8. 기술 스택

- **탐지:** YOLOv11-s, ultralytics, SAHI, Soft-NMS
- **데이터:** OpenCV, ImageHash (pHash), Roboflow, yt-dlp
- **VLM (교사):** Anthropic Claude API
- **VLM (학생):** Qwen2.5-VL-7B, Unsloth, PEFT/LoRA, TRL
- **인프라:** RunPod (RTX 4090 학습, A100 40GB 증류), WSL2 로컬 개발
- **데모:** Streamlit

---

## 9. 규모와 비용

- 기간: 주 20시간 기준 약 3~4주
- 총 비용 예상: $70~140 (RunPod 학습 + Claude API + 증류)
- 증류(5-B) 제외 시 $20~40

---

## 10. 참고 문서 (내부)

- `PLAN.md` — 7단계 로드맵 상세
- `experiments/leak_diagnosis.md` — 1차 실패 진단 (수치 근거)
- `experiments/labeling_plan.md` — 재라벨링 전략
- `experiments/relabel_progress.md` — 재라벨링 진행 상황
- `experiments/history.csv` — 1차 시도 19회 실험 기록
- `experiments/viz/` — 라벨/예측 시각화 (자소서에 스크린샷 사용 가능)
