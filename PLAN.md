# 이차전지 전극 공정 영상 자동 분석 시스템

취업 포트폴리오용 — 객체탐지 기반 공정 구간 인덱싱 + VLM 리포트 생성

---

## 1. Context — 왜 이 프로젝트이고, 왜 배터리인가

### 배경

기존에 배터리 제조 공정 영상(36분, 전 공정 포함)으로 객체탐지를 시도했으나 성능이 나오지 않아 중단된 상태다. 진단 결과 실패 원인은 도메인 난이도가 아니라 다음 세 가지였다.

| 원인 | 내용 |
|---|---|
| **데이터량** | 136장 / 9클래스 = 클래스당 평균 15장. 실무 기준(클래스당 100~150 인스턴스)의 1/10 수준. 어떤 도메인이든 이 조건에서는 실패한다. |
| **증강에 대한 오해** | Roboflow 증강은 기존 이미지의 변형이지 새 정보가 아니다. 3배 증강해도 정보량은 136장어치 그대로이며, 미학습 영상에 대한 일반화는 개선되지 않는다. |
| **클래스 설계** | `separator`(얇은 필름), `coating electrode`(면 형태)는 바운딩박스에 부적합. `press roller`와 `slitting blade`는 둘 다 금속 원통이라 문맥 없이는 사람도 구분 불가 → 라벨 자체에 모순이 섞여 있었을 가능성이 높다. |

한편 train/val 분할은 각 공정 구간 내에서 시간적으로 분리해 수행했다. 무작위 프레임 분할이라는 흔한 함정은 피한 상태이므로, 분할 방식은 실패 원인이 아니다.

### 배터리를 유지해야 하는 이유

"배터리가 아니어도 좋다"고 하셨으나, **이차전지 소재 전공 + 연구원 경력**이 있다면 배터리가 압도적으로 유리하다.

- **차별화** — YOLO + 공개 데이터셋 포트폴리오는 수백 명이 만든다. 배터리 공정 CV를 배터리 소재 연구 경력자가 만든 사례는 거의 없다.
- **경력 서사의 연결** — "소재 연구원이 공정 데이터를 다루는 AI 엔지니어로 전환" 이라는 이야기가 성립한다. 도메인을 바꾸면 이 연결이 끊기고 경력이 무관해진다.
- **검증 가능성** — 이 프로젝트에서 가장 방어하기 좋은 지점은 **VLM 출력의 사실 정확성을 직접 채점할 수 있다**는 점이다. 공정을 아는 사람만 할 수 있는 평가이며, 이것이 다른 지원자와 갈리는 부분이다.
- **타깃** — LG에너지솔루션·삼성SDI·SK온의 스마트팩토리/AI 조직, 이차전지 장비사(피엔티·씨아이에스·한화모멘텀), 제조 AI 기업의 도메인 팀.

**공개 데이터가 없어 다들 피하는 분야를 고른 것은 잘못이 아니다. 데이터를 136장에서 멈춘 것이 문제였다.**

### 기존 컨셉의 치명적 결함과 재설계

원래 구상은 "공정·장비를 탐지 → VLM이 해당 공정을 설명"이었다. 여기에는 구조적 문제가 있다.

> **탐지기와 VLM이 서로를 필요로 하지 않는다.**

VLM은 프레임만 봐도 코팅 라인을 알아보므로 탐지 결과가 없어도 같은 설명을 내놓는다. 또한 "코팅은 슬러리를 집전체에 도포하는 공정"이라는 서술은 이미지와 무관한 교과서 지식이라, 프레임이 바뀌어도 출력이 변하지 않는다. 즉 VLM이 하는 일이 사실상 딕셔너리 조회가 된다. 면접에서 "탐지기가 왜 필요한가요"라는 질문에 답할 수 없다.

**재설계 — 36분 전 공정 영상이라는 기존 자산을 강점으로 전환한다.**

```
36분 영상
   │
   ├─[1] 탐지기: 프레임별 설비·부품 인식        ← VLM이 못 하는 일
   │        (36분 전체를 구조화된 시퀀스로)
   │
   ├─[2] 시계열 집계: 프레임 단위 공정 분류 → 평활화 → 구간 도출
   │        코팅·캘린더링·슬리팅 0:34–12:50 / 와인딩·캡슐화 12:50–30:25 / 조립·테스트 30:25–36:05
   │
   ├─[3] VLM: 구간별 근거 기반 리포트 생성      ← 탐지기가 못 하는 일
   │        (탐지된 설비 목록·전환 시점을 입력으로 받음)
   │        Claude(교사) → 소형 VLM 증류(온프레미스)
   │
   └─[4] 출력: 공정 타임라인 + 구간별 리포트 + 검색 인덱스
```

이 구조에서는 역할 분담이 명확하다. **36분 영상을 통째로 VLM에 넣을 수 없으므로 탐지기가 반드시 필요하고**, 구조화된 탐지 결과를 사람이 읽는 서술로 바꾸려면 VLM이 반드시 필요하다.

**용도:** 신입 교육자료 자동 생성 / 공정 영상 아카이브 검색("슬리팅 장면만 찾아줘") / 설비 점검 이력 문서화.

---

## 2. 단계별 실행 계획

### Phase 0 — 기존 데이터 진단 (1일)

목적: "데이터 부족이 원인"이라는 진단을 수치로 확인하고, 어느 클래스가 학습을 망치는지 특정한다.

- 기존 136장 데이터셋을 로컬로 내보내기 (Roboflow → YOLO format)
- 분석 스크립트 작성 (`scripts/analyze_dataset.py`)
  - 클래스별 인스턴스 수 분포
  - 박스 크기 분포 (전체 이미지 대비 면적 비율) → 소형 객체 비중 확인
  - 박스 종횡비 분포 → 박스 부적합 클래스 식별
  - 이미지당 박스 개수, 박스 간 IoU 분포 → 밀집/중첩 정도 정량화
- 기존 학습 결과가 있다면 클래스별 AP 확인
- **하이라이트 구간(0:00~0:34) 프레임이 학습 데이터에 섞였는지 확인** — 편집 몽타주라 여러 공정이 뒤섞여 있어 오염원이 된다. 섞였다면 성능 저하 요인이 하나 더 특정된다

**산출물:** 어떤 클래스를 살리고 병합·삭제할지에 대한 근거 데이터.

---

### Phase 1 — 클래스 재설계 + 베이스라인 (2~3일)

**재설계 원칙 — 세 조건을 모두 만족하는 클래스만 남긴다.**

1. 바운딩박스로 표현 가능한가 (면·필름 형태 제외)
2. 형태만으로 다른 클래스와 구분되는가 (사람이 헷갈리면 모델도 헷갈린다)
3. **공정 구간을 특정하는 데 기여하는가** ← 이번 아키텍처의 핵심 기준

기존 9개 중 확인된 4개에 대한 판정:

| 기존 클래스 | 판정 | 사유 |
|---|---|---|
| `coating electrode` | 제외 또는 재정의 | 면 형태. 대신 `coating_die`(다이 코터 헤드)로 대체 — 박스 표현 가능하고 코팅 구간을 특정함 |
| `separator` | 제외 | 얇은 필름, 경계 모호 |
| `press roller` | 유지 → `roll_press` | 캘린더링 구간 특정 |
| `slitting blade` | 유지 → `slitting_knife` | 슬리팅 구간 특정 |

**목표 클래스 (4~5개) — Phase 0 결과와 나머지 5개 클래스 확인 후 확정:**

- `coating_die` — 코팅 구간
- `roll_press` — 캘린더링 구간
- `slitting_knife` — 슬리팅 구간
- `winding_core` — 와인딩 구간
- `web_roll` — 공통 이송 롤 (라인 추적용, 선택)

> `roll_press`와 `slitting_knife`가 여전히 혼동된다면 두 클래스를 `roller`로 병합하고, **구간 판별은 시계열 문맥에 맡긴다.** 순서가 압연 → 슬리팅으로 고정되어 있으므로 시간 정보로 구분 가능하다. 도메인 지식을 알고리즘에 넣는 지점이라 서술 가치가 높다.

**작업**

- 기존 136장의 라벨을 클래스 매핑 스크립트로 재구성 (`scripts/remap_classes.py`) — 재라벨링 불필요
- YOLOv11-s 베이스라인 학습, `imgsz=1280`
- **Phase 0 진단 검증:** 클래스 축소만으로 mAP가 유의미하게 오르면 데이터·클래스 설계가 원인이었음이 확인된다

---

### Phase 2 — 데이터 확장 (5~7일, 가장 오래 걸림)

**목표: 600~800장 / 영상 3~5개**

**기준 영상 확인 결과**

「세계 최초의 반고체 배터리 파워뱅크」(Miracle Process 및 Marasone, 36:05, 중국 공장 대량생산 라인)

| 시각 | 챕터 | 처리 |
|---|---|---|
| 0:00 | 하이라이트 | **제외** — 여러 공정 클립을 편집한 몽타주. 학습 데이터 오염원 |
| 0:34 | 배터리 코팅 & 캘린더링 & 슬리팅 | 사용 (12분 16초) |
| 12:50 | 배터리 와인딩 & 캡슐화 | 사용 (17분 35초) |
| 30:25 | 배터리 조립 & 테스트 | 사용 (5분 40초) |

**챕터 = 무료 프레임 라벨.** 1fps 추출 시 약 2,160 프레임에 공정 라벨이 수작업 없이 자동으로 붙는다. Phase 4의 학습·평가 데이터가 여기서 나온다.

**영상 확보 — "챕터가 달린 영상"을 선별 기준으로 삼는다**

- 기존 36분 영상 외 전극/셀 제조 공정 영상 2~4개 추가 (YouTube)
- **챕터 유무를 최우선 기준으로** 선택 — 라벨 비용 없이 데이터가 늘어난다
- 원본 영상은 저장소에 커밋하지 않는다 (약관 회색지대). 추출 프레임 일부만 사용하고 `data/` 는 `.gitignore` 처리

**프레임 추출** (`scripts/extract_frames.py`)

- 2~3초 간격 샘플링, **하이라이트/인트로 구간은 시각 범위로 배제**
- **perceptual hash 기반 중복 제거** — 인접 프레임 유사도가 임계값 이상이면 폐기
- 챕터 메타데이터를 파싱해 프레임별 공정 라벨을 자동 부착 (`yt-dlp --write-info-json`)
- 10분 영상당 100~200장이 남는 것이 정상

**분할 전략 — 반드시 영상 단위로 분할한다**

```
train : 영상 1(기존 36분), 2, 3
val   : 영상 4
test  : 영상 5   ← 학습에 전혀 쓰지 않은 영상
```

한 영상 안에서 프레임을 나누면 카메라·조명·설비가 동일해 점수가 부풀려진다. 영상 단위 분할의 mAP는 낮게 나오지만 **그것이 진짜 숫자**이며, 이 선택 자체가 README의 서술 포인트가 된다.

**반자동 라벨링 (self-training)**

1. Phase 1 베이스라인 모델로 신규 프레임에 의사라벨 생성
2. Roboflow에 업로드해 **틀린 것만 수정** (처음부터 그리기보다 3~4배 빠름)
3. 수정 데이터로 재학습 → 다음 배치에 의사라벨 생성 → 반복 (2~3 라운드)

> Grounding DINO·YOLO-World 같은 open-vocabulary 자동라벨링은 이 프로젝트에 쓰지 않는다. `slitting knife`, `coating die` 같은 산업 설비 용어가 사전학습 어휘에 없어 성능이 나오지 않는다.

---

### Phase 3 — RunPod 학습 및 튜닝 (3~4일)

**환경**

- RunPod GPU: RTX 4090 24GB 또는 A5000 (시간당 $0.3~0.5 수준, 실제 요금은 콘솔에서 확인)
- 학습 1회 1~2시간 → 회당 $1 미만. 실험 10~15회 잡아도 총 $10~20 수준
- 데이터는 RunPod Network Volume 또는 학습 시작 시 S3/Google Drive에서 내려받는 방식

**설정 및 튜닝 레버 (중요도 순)**

| 항목 | 값 | 근거 |
|---|---|---|
| 모델 | YOLOv11-s | 데이터가 적을수록 사전학습 특징이 중요. n보다 s 권장 |
| `imgsz` | 1280 | 640에서는 설비 부품이 20~30px로 축소되어 특징이 소실됨 |
| **SAHI 타일링** | 추론 시 적용 | 이미지를 겹쳐 잘라 추론 후 병합. 밀집·소형 객체에서 가장 큰 개선폭 |
| NMS | Soft-NMS 또는 IoU 임계값 상향 | 중첩된 설비의 정답 박스가 표준 NMS에서 삭제되는 문제 대응 |
| 증강 | mosaic 유지, 색상 증강 축소 | 설비 색상은 판별 단서이므로 과도한 색 변형은 해가 됨 |

**실험 기록** — 설정·mAP·소요시간을 `experiments/` 에 CSV로 축적. 포트폴리오에 튜닝 과정을 표로 제시한다.

---

### Phase 4 — 프레임 단위 공정 분류 + 구간 도출 (2~3일)

**설계 변경 근거 (영상 확인 결과 반영)**

기준 영상의 챕터가 3개뿐(하이라이트 제외)이므로 **구간 경계가 2개**다. 경계 2개로는 분할 성능을 평가할 수 없다. 따라서 경계 탐지 대신 **프레임 단위 공정 분류**를 1차 과제로 삼는다. 프레임 약 2,000개에 대한 분류 정확도는 통계적으로 의미가 있고, **구간 경계는 분류 결과를 평활화하면 부산물로 얻어진다.**

**파이프라인** (`src/segmentation.py`)

1. 프레임별 탐지 결과 → 클래스별 존재/개수 벡터
2. 공정 분류 — 탐지 벡터 기반 (규칙 또는 경량 분류기). 라벨은 챕터에서 자동 생성
3. 시간축 이동평균 + 히스테리시스로 평활화 — 순간적 오탐 제거, 경계 떨림 방지
4. 평활화된 시퀀스에서 구간 경계 추출
5. **도메인 지식 제약 적용** — 전극 공정 순서(코팅 → 캘린더링 → 슬리팅 → 와인딩 → 조립)는 고정이므로, 순서에 어긋나는 전환을 후처리로 교정

> 5번이 전공 지식이 알고리즘에 직접 들어가는 지점이다. 단순 CV 프로젝트와 갈리는 부분이므로 README에 명시한다.

**평가:** 프레임 분류 정확도 / 혼동행렬 (주 지표), 구간 경계 오차 초 단위 (보조 지표, 영상 수가 늘면 신뢰도 상승)

---

### Phase 5 — VLM 리포트 생성 + 온프레미스 증류 (5~7일)

#### 5-A. Claude 기반 리포트 생성 (교사 모델)

**백엔드:** Claude API (`claude-sonnet-5`). 프레임당 비용은 수십 원 수준.

**입력 — grounding이 핵심이다**

- 구간 대표 프레임 2~3장
- **구조화된 탐지 요약** — 예: `{구간: 12:50–30:25, 지배 클래스: winding_core(평균 conf 0.87, 연속 142프레임), 동반: web_roll, 직전 구간: 슬리팅}`
- 도메인 컨텍스트 프롬프트 ← **전공 지식을 투입하는 지점**

**출력 스키마 고정 (JSON)**

```json
{
  "process_name": "와인딩",
  "time_range": "12:50–30:25",
  "observed_equipment": ["winding_core", "web_roll"],
  "description": "...",
  "quality_factors": ["권취 장력 편차", "전극 정렬 불량", "..."],
  "evidence_frames": [12450, 13200]
}
```

**환각 억제 규칙 (프롬프트에 명시)**

- 탐지되지 않은 설비는 언급 금지
- 모든 주장에 근거 프레임 번호 인용 요구
- 판단 불가 시 `"unknown"` 반환 허용 — 억지 서술 금지

**대조 실험** — 조건 A(프레임만) vs 조건 B(프레임 + 탐지 결과). B가 유의미하게 우수해야 아키텍처가 정당화된다. 아니라면 탐지 결과 주입 방식을 재설계한다.

#### 5-B. 소형 VLM 증류 (학생 모델) — 온프레미스 대응

**왜 파인튜닝하는가 — 기술 과시가 아니라 요구사항이다**

> 배터리 제조사는 라인 구성과 설비 배치가 영업비밀이므로 **공정 영상을 외부 API로 전송할 수 없다.** 온프레미스 VLM은 이 도메인의 실제 도입 조건이다.

**데이터 문제와 해법**

VLM 파인튜닝은 (이미지, 텍스트) 쌍이 수백~수천 개 필요한데 직접 작성은 비현실적이다. 또한 샘플이 적으면 카타스트로픽 포게팅으로 **베이스 모델보다 나빠진다.** 따라서 증류 방식으로 우회한다.

1. 5-A의 Claude 출력으로 학습 쌍 500~1,000개 생성
2. **도메인 전문가로서 직접 검수·수정** ← 품질 보증이자 전공 투입 지점
3. LoRA 파인튜닝
4. 3자 비교 평가

**모델 선택**

| 후보 | 판정 |
|---|---|
| **Qwen2.5-VL-7B** | **권장.** 단일 24GB GPU LoRA에 가장 검증된 경로, 산업·문서 이미지 강함, 한국어 양호 |
| Qwen3-VL 계열 | 2026년 현재 오픈소스 최상위권. 소형 변형 가용 시 우선 검토 |
| Llama 3.2 Vision 11B | 한국어가 약해 한국어 리포트에 불리. 비교 대상으로만 (우선순위 낮음) |

**툴체인:** Unsloth (Llama-3.2-Vision·Qwen2.5-VL 지원, 속도 2배·메모리 70% 절감)
**LoRA 설정:** ViT와 vision-language merger는 동결하고 LLM 디코더에만 적용 (`q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj`)
**GPU:** RunPod A100 40GB 권장 (4090 24GB도 QLoRA로 가능하나 빠듯)

**최종 서사**

> 공정 영상은 보안상 외부 전송이 불가능하다. Claude로 학습 데이터를 생성하고 도메인 전문가가 검수한 뒤 폐쇄망 배포가 가능한 소형 VLM으로 증류했다. Claude 대비 성능 XX%를 유지하면서 완전 온프레미스 운영이 가능하다.

---

### Phase 6 — 평가 · 데모 · 문서화 (3~4일)

**평가 — 세 층으로 나눈다**

| 대상 | 지표 |
|---|---|
| 탐지 | mAP50, mAP50-95, 클래스별 AP (**영상 단위 분할 기준**) |
| 공정 분류 | 프레임 단위 정확도 + 혼동행렬 (주 지표), 구간 경계 오차 초 단위 (보조) |
| **VLM 리포트** | **도메인 전문가 채점 루브릭 — 3자 비교** |

**VLM 3자 비교**

| 모델 | 역할 |
|---|---|
| Qwen2.5-VL-7B 베이스 | 파인튜닝 전 기준선 |
| Qwen2.5-VL-7B 증류 | 제안 모델 (온프레미스 배포 대상) |
| Claude Sonnet | 상한선 (교사 모델) |

증류 모델이 베이스보다 확실히 우수하고 Claude에 근접할수록 좋다. **증류 모델이 베이스보다 나쁘면 데이터 부족으로 인한 카타스트로픽 포게팅**이므로 학습 쌍을 늘리거나 LoRA rank를 낮춘다.

**VLM 평가 루브릭 — 이 프로젝트의 킬러 요소**

각 항목 1~5점, 구간별로 직접 채점:

1. **사실 정확성** — 공정 설명이 실제와 부합하는가
2. **근거성** — 탐지 결과에 기반했는가, 환각이 없는가
3. **실무 유용성** — 현장에서 쓸 만한 내용인가

> "이차전지 소재 연구원 출신이 직접 설계하고 채점한 VLM 평가"는 다른 지원자가 재현할 수 없는 자산이다. 이 루브릭과 채점 결과를 README에 표로 넣는 것이 **경력과 프로젝트를 잇는 가장 강한 연결고리**다.

**데모:** Streamlit — 영상 업로드 → 공정 타임라인 → 구간 클릭 시 리포트 → 검색. Streamlit Cloud 무료 배포로 링크 공유.

**README 구성 — 실패를 숨기지 않는다**

```
문제 정의 → 1차 시도(136장/9클래스) → 실패 → 원인 진단(수치) →
재설계(클래스·데이터·아키텍처) → 결과 → 한계와 다음 단계
```

136장 실패를 그대로 쓰고 진단 근거를 수치로 제시하면 **"문제를 정의하고 재설계할 줄 아는 사람"** 이라는 인상을 준다. 처음부터 잘된 프로젝트보다 강한 서사다.

---

## 3. 기술 스택 및 환경

**현재 환경 확인 결과**

| 항목 | 상태 |
|---|---|
| Python 3.11 | 설치됨 (`py` 런처만, PATH 미등록 → 등록 필요) |
| Node / Git | 사용 가능 |
| Docker | 없음 (RunPod 사용하므로 불필요) |
| 로컬 GPU | GeForce MX450 2GB — **추론·데이터 준비 전용**, 학습 불가 |
| RAM / 디스크 | 16GB / 158GB 여유 — 충분 |

**역할 분담:** 로컬은 데이터 준비·추론·UI, RunPod은 학습만.

**스택:** `ultralytics` (YOLOv11), `sahi`, `opencv-python`, `imagehash`, `anthropic`, `streamlit`, `yt-dlp`
**증류 단계 추가:** `unsloth`, `transformers`, `peft`, `trl`

---

## 4. 프로젝트 구조

```
battery-process-vlm/
├── data/                     # .gitignore (영상·프레임 원본)
│   ├── raw_videos/
│   ├── frames/
│   └── dataset/              # YOLO format, 영상 단위 분할
├── scripts/
│   ├── analyze_dataset.py    # Phase 0 진단
│   ├── remap_classes.py      # Phase 1 클래스 재구성
│   ├── extract_frames.py     # Phase 2 추출 + pHash 중복 제거 + 챕터 라벨 부착
│   ├── pseudo_label.py       # Phase 2 의사라벨 생성
│   └── build_sft_dataset.py  # Phase 5-B 증류용 학습 쌍 구축
├── src/
│   ├── detector.py           # YOLO 추론 + SAHI 타일링
│   ├── segmentation.py       # Phase 4 공정 분류 + 구간 도출 + 도메인 제약
│   ├── vlm_reporter.py       # Phase 5-A Claude API 리포트
│   ├── vlm_local.py          # Phase 5-B 증류 모델 추론
│   └── pipeline.py           # 전체 오케스트레이션
├── experiments/              # 학습 설정·결과 CSV
├── runpod/
│   ├── train_detector.py
│   ├── train_vlm_lora.py     # Phase 5-B Unsloth LoRA
│   └── setup.sh
├── app.py                    # Streamlit 데모
└── README.md
```

---

## 5. 검증 방법

**Phase 0~1 — 진단 가설 검증**

```bash
py -3.11 scripts/analyze_dataset.py --data data/dataset
# 기대: 클래스별 인스턴스 수 불균형, 소형 객체 비중, 박스 부적합 클래스가 수치로 드러남

py -3.11 scripts/remap_classes.py --from 9 --to 5
# 클래스 축소 후 재학습 → mAP50 상승 확인 (진단이 맞다면 유의미하게 오름)
```

**Phase 2 — 데이터 파이프라인**

```bash
py -3.11 scripts/extract_frames.py --video data/raw_videos/v2.mp4 --interval 2.5 --dedup
# 검증: 출력 프레임을 눈으로 훑어 중복이 제거됐는지, 공정 구간이 고르게 포함됐는지 확인
# 검증: dataset split이 영상 단위로 분리됐는지 (train/val/test에 같은 영상 프레임이 섞이면 실패)
```

**Phase 3 — 학습**

- RunPod에서 학습 후 `test` 영상(미학습)에 대한 mAP 확인
- val과 test의 mAP 격차가 크면 여전히 과적합 → 데이터 추가 필요
- SAHI 적용 전후 mAP 비교표 작성

**Phase 4 — 구간 분할**

- 36분 영상의 실제 공정 전환 시점을 수동으로 기록해 정답 생성
- 자동 분할 결과와 비교, 경계 오차를 초 단위로 측정
- 경계가 떨리면 평활화 윈도우·히스테리시스 조정

**Phase 4 — 공정 분류**

```bash
py -3.11 -m src.segmentation --video data/raw_videos/v1.mp4 --eval
# 검증: 챕터 라벨 대비 프레임 분류 정확도 + 혼동행렬
# 검증: 평활화 후 경계가 챕터 시각(0:34 / 12:50 / 30:25)에 근접하는지
```

**Phase 5-A — Claude 리포트**

- 조건 A(프레임만) vs B(프레임+탐지) 출력을 나란히 놓고 루브릭 채점
- B가 A보다 유의미하게 높아야 아키텍처가 정당화됨. **아니라면 탐지 결과 주입 방식을 재설계**
- 환각 검사: 탐지되지 않은 설비를 언급한 사례 수를 카운트

**Phase 5-B — 증류 모델**

- 베이스 / 증류 / Claude 3자 출력을 동일 루브릭으로 채점
- **증류 < 베이스이면 중단하고 학습 쌍을 늘린다** — 데이터 부족으로 인한 카타스트로픽 포게팅 신호
- JSON 스키마 준수율 측정 — 파인튜닝의 가장 확실한 효과는 출력 형식 안정화로 나타난다

**Phase 6 — 통합**

```bash
py -3.11 -m streamlit run app.py
# 미학습 영상을 업로드해 타임라인·리포트가 끝까지 생성되는지 확인
```

---

## 6. 일정 및 비용

**주당 20시간 기준 약 3~4주**

| Phase | 기간 | 비고 |
|---|---|---|
| 0. 진단 | 1일 | |
| 1. 클래스 재설계 + 베이스라인 | 2~3일 | 진단 가설 검증 지점 |
| 2. 데이터 확장 | 5~7일 | **최대 병목 — 라벨링** |
| 3. RunPod 탐지 학습 | 3~4일 | |
| 4. 공정 분류 + 구간 도출 | 2~3일 | 라벨은 챕터에서 자동 |
| 5. VLM 리포트 + 증류 | 5~7일 | 5-B는 선택적 확장 |
| 6. 평가·데모·문서 | 3~4일 | |

**비용**

| 항목 | 금액 |
|---|---|
| RunPod 탐지 학습 (4090, 실험 10~15회) | $10~20 |
| Claude API (리포트 생성 + 학습 데이터 구축) | $10~20 |
| RunPod VLM 증류 (A100 40GB, 5~10회 × 3~5h) | $50~100 |
| **합계** | **$70~140** |

> 5-B(증류)를 빼면 $20~40으로 떨어진다. **Phase 5-A까지 완성해 동작하는 시스템을 먼저 확보한 뒤 5-B를 진행**하는 것을 권장한다. 증류는 가치가 크지만 없어도 프로젝트는 성립하며, 5-A가 5-B의 학습 데이터 공급원이므로 순서를 뒤집을 수 없다.

---

## 7. 시작 전 확정 필요 사항

1. **기존 9개 클래스의 전체 목록** — 확인된 4개(coating electrode, slitting blade, separator, press roller) 외 나머지 5개. Phase 1 클래스 확정에 필요
2. **기존 데이터 위치** — Roboflow 워크스페이스인지 로컬 내보내기 상태인지
3. **RunPod 계정 보유 여부** — Phase 3은 4090, Phase 5-B는 A100 40GB
4. **Anthropic API 키 보유 여부**
5. **Phase 5-B(증류) 진행 여부** — 비용이 $20~40에서 $70~140으로 오른다. Phase 5-A 완료 시점에 결정해도 늦지 않다

1·2번은 Phase 0에서 바로 필요하고, 3·4번은 Phase 3·5 시작 전까지 준비하면 된다.

---

## 8. 이 계획이 기존 시도와 다른 점 (요약)

| 항목 | 1차 시도 | 이번 계획 |
|---|---|---|
| 데이터 | 136장 (1개 영상) | 600~800장 (3~5개 영상) |
| 클래스 | 9개 (박스 부적합·혼동 클래스 포함) | 4~5개 (박스 적합 + 구간 특정 기여) |
| 분할 | 영상 내 구간별 시간 분리 | **영상 단위 분리** (미학습 영상으로 테스트) |
| 하이라이트 구간 | 미확인 (오염 가능) | 명시적 배제 |
| 라벨링 | 전수 수작업 | 의사라벨 + 수정 (탐지) / 챕터 자동 (공정) |
| 아키텍처 | 탐지 → VLM 설명 (상호 독립) | 탐지 → 구간화 → VLM (상호 의존) |
| VLM | 미구현 | Claude + 온프레미스 증류 |
| 평가 | mAP | mAP + 분류 정확도 + **도메인 전문가 루브릭** |
